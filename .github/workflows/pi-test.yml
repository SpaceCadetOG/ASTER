name: runner-test

on:
  push:
    branches: ["main"]

jobs:
  test:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: show system info
        run: |
          echo "Runner works"
          uname -a
          whoami